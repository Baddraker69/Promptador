import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Promptador — AI Prompt Engineering",
  description:
    "Generate, optimise, and manage AI prompts for agentic teams, image generation, and more.",
  keywords: ["prompt engineering", "AI prompts", "agentic AI", "image generation", "Claude", "GPT"],
  openGraph: {
    title: "Promptador",
    description: "Professional AI prompt engineering, beautifully organised.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans`}>
        {children}
      </body>
    </html>
  );
}
